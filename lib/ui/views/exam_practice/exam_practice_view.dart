import 'package:flutter/material.dart';

class ExamPracticeView extends StatelessWidget {
  const ExamPracticeView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
