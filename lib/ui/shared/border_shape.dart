import 'package:flutter/material.dart';

enum border_shape { Boxy, SmoothCurve, Rounded }

final BorderRadius boxy = BorderRadius.circular(5.0);
final BorderRadius smoothCurve = BorderRadius.circular(10.0);
final BorderRadius rounded = BorderRadius.circular(20.0);

final double elevation = 3;